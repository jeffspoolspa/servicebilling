summary: 'DISCOVERY: is a working chromium pre-baked on the image (immune to apt-150)?'
description: find every chrome binary on the read-only image, version-check, render-test the non-150 one.
tag: chromium
concurrency_key: ion_chromium
concurrency_time_window_s: 5
concurrent_limit: 1
kind: script
schema:
  $schema: 'https://json-schema.org/draft/2020-12/schema'
  type: object
  properties: {}
  required: []
