summary: 'DISCOVERY: can a job self-provision the pinned chromium build (no worker config)?'
description: >-
  Downloads playwright build 1091 (the 1.40 match) inside the job, extracts
  via python3 zipfile, chmods, and test-renders. Probes shared-cache
  writability for cross-job reuse.
tag: chromium
kind: script
schema:
  $schema: 'https://json-schema.org/draft/2020-12/schema'
  type: object
  properties: {}
  required: []
